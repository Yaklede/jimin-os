<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Only the ADB-installed debug APK accepts cleartext. Its test origin is
         hard-pinned at build time to the USB-reversed loopback address. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
